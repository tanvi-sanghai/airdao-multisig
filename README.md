# AirDAO Multisig Contracts

Start with installing all the dependencies

```shell
npm i --force
```

Then execute this command to compile and deploy your contracts

```shell
npx hardhat run scripts/deployFactory.js --network testnet
```
